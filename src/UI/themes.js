import { lightBackground, lightBoxInsideBackground, lightTextColor, darkBackground, darkTextColor, darkBoxInsideBackground } from "./index"; 

export const darkTheme = {
    body: darkBackground,
    inside: darkBoxInsideBackground,
    text: darkTextColor
}

export const lightTheme = {
    body: lightBackground,
    inside: lightBoxInsideBackground,
    text: lightTextColor
}